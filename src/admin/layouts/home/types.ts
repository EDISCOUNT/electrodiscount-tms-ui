export enum ShipmentFilterDateType {
    TODAY,
    THIS_WEEK,
    THIS_MONTH,
    THIS_YEAR,
    EVER,
    DATE_SELECT,
    DATE_RANGE_SELECT,
}

export enum ShipmentFilterDateEndType {
    EXPECTED,
    DELIVERED,
}
